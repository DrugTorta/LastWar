[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "./server"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

# Настройка Volume для постоянного хранения базы данных
[[deploy.volumes]]
mountPath = "/data"
name = "database-volume"
